const CentenaryBadge = () => (
  <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-primary/20 bg-primary/5">
    <span className="text-xs font-display font-semibold text-primary tracking-wide">
      🏛️ 100 Years of Excellence
    </span>
  </div>
);

export default CentenaryBadge;
